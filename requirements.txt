# ── Web framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart

# ── Data models ───────────────────────────────────────────────────────────────
# Pydantic v2 is pinned because v1 and v2 have incompatible APIs.
# Our models.py uses v2 syntax (model_dump, field validators).
pydantic>=2.0

# ── Data processing ───────────────────────────────────────────────────────────
pandas
numpy

# ── LLM providers (install all, unused ones are never imported) ───────────────
# OpenAI — AsyncOpenAI used in llm_service
openai

# AWS Bedrock — boto3 Session + bedrock-runtime client
boto3

# Google Gemini — google-genai >= 1.0 required for typed GenerateContentConfig
# (bare dict config and string contents broke in 1.0)
google-genai>=1.0

# ── HTTP client (used in inference.py to call the env server) ─────────────────
requests

# ── YAML parsing (openenv.yaml + validate_local.py) ──────────────────────────
pyyaml
python-dotenv

openenv-core
uv